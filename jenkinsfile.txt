pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                bat 'dotnet build SimpleCoreWebAPI.sln -p:configuration=release -v:n'
                echo "Building......."
            }
        }
	stage('Test') {
	    steps {
		bat 'dotnet test'
	    }
	}
	stage('Publish') {
	    steps {
			bat 'dotnet publish'
	    }
	}
     }
     post {
	success {
		archiveArtifacts artifacts: '**', fingerprint: true
		bat 'dotnet SimpleCoreWebAPI/bin/Debug/netcoreapp2.1/SimpleCoreWebAPI.dll'
	}
    }
}
