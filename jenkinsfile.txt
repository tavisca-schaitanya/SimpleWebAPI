pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'dotnet build SimpleCoreWebAPI.sln -p:configuration=release -v:n'
                echo "Building......."
            }
        }
     }
}
